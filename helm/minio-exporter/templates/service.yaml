apiVersion: v1
kind: Service
metadata:
  name: {{ include "minio-exporter.fullname" . }}
  labels:
    {{- include "minio-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minio-exporter.selectorLabels" . | nindent 4 }}
